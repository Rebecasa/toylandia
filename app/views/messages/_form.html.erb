<div>
    <%= simple_form_for([@booking, @booking.messages.new], id: 'new_message', remote: true, :method => :post) do |f| %>
      <%= f.input :content, as: :text , label: false%>
      <%= f.submit class: "button-form" %>
    <% end %>
</div>
